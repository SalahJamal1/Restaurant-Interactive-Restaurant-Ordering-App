server:
  port: ${PORT:8080}
spring:
  datasource:
    url: jdbc:mysql://sql8.freesqldatabase.com:3306/sql8779689
    username: sql8779689
    password: rxyyDbRp9C
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
